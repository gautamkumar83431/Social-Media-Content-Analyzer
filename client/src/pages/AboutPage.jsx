import React from 'react';
export default function AboutPage(){ return (<div className='page'><h2>About</h2><p>This app extracts text from PDFs and images (OCR), runs simple heuristics to suggest engagement improvements, and stores history locally on the server in <code>history.json</code>.</p></div>); }
